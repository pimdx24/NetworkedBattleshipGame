{
  "name": "battleship-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev":    "node -e \"const fs=require('fs');if(!fs.existsSync('node_modules')){console.error('\\n  ERROR: node_modules not found.\\n  Run: npm install\\n');process.exit(1)}\"",
    "dev":       "vite",
    "build":     "vite build",
    "preview":   "vite preview"
  },
  "dependencies": {
    "react":     "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite":                 "^5.4.2"
  },
  "engines": {
    "node": ">=20",
    "npm":  ">=9"
  }
}
